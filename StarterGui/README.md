# StarterGui

This project creates the shop ScreenGui at runtime from:

- `StarterPlayer/StarterPlayerScripts/UpgradeShopClient.client.lua`

It uses Roblox GUI instances such as `ScreenGui`, `Frame`, `TextLabel`, `TextButton`, and `ViewportFrame`.
